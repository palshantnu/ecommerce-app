# E-commerce Backend (Node + Express + Sequelize + MySQL)

This is a scaffold generated for a React Native e-commerce app backend.
It uses Sequelize with MySQL and does not include token-based authentication.

## Setup
1. Copy `.env.example` to `.env` and fill DB credentials.
2. `npm install`
3. `npm run dev`
